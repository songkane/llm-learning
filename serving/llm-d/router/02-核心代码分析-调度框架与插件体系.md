# 02 · 调度框架与插件体系

> **源码基线**：[`main @ 90a28bc`](https://github.com/llm-d/llm-d-router/tree/90a28bc66f1d96f84f8f18f11dcd6ed15f34e830)
> [01 篇](01-请求的一生-主控制流.md) 讲了编排骨架，本篇讲骨架上挂的东西：八类扩展点、YAML 如何变成插件实例、以及打分到底怎么算。
> `pkg/epp/framework` 是全仓库最大的目录（38090 行 / 234 个非测试文件），几乎全是插件。

## 0. 一句话定位

EPP 的**核心代码不做决策**。01 篇看到的 `Director` 和 `Scheduler` 只有约 2400 行，它们唯一的工作是「按顺序调用插件」。所有实际行为都在插件里。

推论：**读核心代码看不出 EPP 的行为，必须配着 `EndpointPickerConfig` 看。** 这也是排障第一步——把生效的配置 dump 出来。

## 1. 八类扩展点

### 1.1 基础契约

所有插件都嵌入同一个最小接口：

```go
// pkg/epp/framework/interface/plugin/plugins.go:23-89（节选）
type Plugin interface {
	TypedName() TypedName          // {Type, Name}，Type 是 YAML 里的 type，Name 是实例名
}

type ConsumerPlugin interface {
	Plugin
	Consumes() DataDependencies    // 声明「我要读哪些数据」
}

type ProducerPlugin interface {
	Plugin
	Produces() map[DataKey]any     // 声明「我产出哪些数据」
}
```

`Consumes` / `Produces` 是 Data Layer 的数据依赖声明，用来做两件事：**构建插件实例化的拓扑序**、**在运行时拦截未声明的读写**（01 篇 §7 那个「静默丢弃」的坑就源于此）。

### 1.2 调度层四类

```go
// pkg/epp/framework/interface/scheduling/plugins.go:43-78
type ProfileHandler interface {
	plugin.Plugin
	// Pick selects the SchedulingProfiles to run from a list of candidate profiles, while taking into consideration the request properties
	// and the previously executed SchedluderProfile cycles along with their results.
	Pick(ctx context.Context, request *InferenceRequest, profiles map[string]SchedulerProfile,
		profileResults map[string]*ProfileRunResult) map[string]SchedulerProfile

	// ProcessResults handles the outcome of the profile runs after all profiles ran.
	// It may aggregate results, log test profile outputs, or apply custom logic. It specifies in the SchedulingResult the
	// key of the primary profile that should be used to get the request selected destination.
	// When a profile run fails, its result in the profileResults map is nil.
	ProcessResults(ctx context.Context, request *InferenceRequest,
		profileResults map[string]*ProfileRunResult) (*SchedulingResult, error)
}

// Filter defines the interface for filtering a list of pods based on context.
type Filter interface {
	plugin.Plugin
	Filter(ctx context.Context, request *InferenceRequest, pods []Endpoint) []Endpoint
}

// Scorer defines the interface for scoring a list of pods based on context.
// Scorers must score pods with a value within the range of [0,1] where 1 is the highest score.
// If a scorer returns value greater than 1, it will be treated as score 1.
// If a scorer returns value lower than 0, it will be treated as score 0.
type Scorer interface {
	plugin.Plugin
	Category() ScorerCategory
	Score(ctx context.Context, request *InferenceRequest, pods []Endpoint) map[Endpoint]float64
}

// Picker picks the final pod(s) to send the request to.
type Picker interface {
	plugin.Plugin
	Pick(ctx context.Context, scoredPods []*ScoredEndpoint) *ProfileRunResult
}
```

注意 `Filter` **没有 error 返回值**。这是个有意的设计约束：Filter 只能「剔除」，不能「让请求失败」。它砍空了候选集，由框架统一转成 429（01 篇 §3.1）。

`Scorer.Category()` 是个纯声明性的东西，标记这个 scorer 的偏好方向：

```go
// pkg/epp/framework/interface/scheduling/plugins.go:28-39（节选）
const (
	// Affinity indicates a scorer that prefers endpoints with existing locality, such as kv-cache or session-related state,
	// and therefore tends to give higher scores to the same endpoints.
	Affinity ScorerCategory = "Affinity"

	// Distribution indicates a scorer that prefers spreading requests evenly across all candidate endpoints to avoid
	// hotspots and improve overall utilization.
	Distribution ScorerCategory = "Distribution"

	// Balance indicates a scorer that its preference is balanced between Affinity and Distribution.
	Balance ScorerCategory = "Balance"
)
```

**这个分类值得记住**，因为它揭示了 scorer 配置的本质矛盾：`prefix-cache-scorer`（Affinity）想把相同前缀的请求都塞给同一个 pod，`queue-scorer`（Distribution）想把请求摊平。**权重配置就是在这两股力之间调平衡**——§3.4 会用统一示例把这场对撞算到小数点后两位，看清楚权重改一点结果就会翻转。

### 1.3 请求控制层五类

```go
// pkg/epp/framework/interface/requestcontrol/plugins.go:50-131（节选）
type Screener interface {
	plugin.Plugin
	Screen(ctx context.Context, request *fwksched.InferenceRequest, endpoints []fwksched.Endpoint) []fwksched.Endpoint
}

type PreRequest interface {
	plugin.Plugin
	PreRequest(ctx context.Context, request *fwksched.InferenceRequest, schedulingResult *fwksched.SchedulingResult) error
}

type DataProducer interface {
	plugin.ProducerPlugin
	Produce(ctx context.Context, request *fwksched.InferenceRequest, pods []fwksched.Endpoint) error
}

type Admitter interface {
	plugin.Plugin
	Admit(ctx context.Context, request *fwksched.InferenceRequest, pods []fwksched.Endpoint) error
}

type RequestHeaderProcessor interface {
	plugin.Plugin
	RequestHeader(ctx context.Context, request *fwksched.InferenceRequest) error
}
```

加上响应侧的 `ResponseHeaderProcessor` / `ResponseBodyProcessor`。扩展点名常量在同文件 `28-36` 行：`RequestHeader` / `Screener` / `Admission` / `DataProducer` / `PreRequest` / `ResponseReceived` / `ResponseStreaming` / `ResponseComplete`。

### 1.4 另外三层

| 层 | 接口 | 定义 | 哪篇讲 |
|---|------|------|--------|
| Data Layer | `DataSource` / `Extractor[T]` / `PollingDispatcher` / `NotificationSource` | `interface/datalayer/plugin.go:31-84`（`NotificationSource` 另在 `:103-114`、`EndpointSource` 在 `:122-128`） | 03 |
| 请求解析 | `Parser` / `ModelNameRewriter` | `interface/requesthandling/plugins.go:33-60` | 本篇 §5.8 |
| Flow Control | `FairnessPolicy` / `OrderingPolicy` / `SaturationDetector` / `UsageLimitPolicy` | `interface/flowcontrol/plugins.go` | 05 |

**合计八大类扩展点**：Screener、Filter、Scorer、Picker、ProfileHandler、DataProducer、Admitter、请求/响应头体处理器；加上 Data Layer、Parser、Flow Control 三个配套层。

## 2. YAML 如何变成运行时插件

### 2.1 注册表

插件类型名（YAML 里的 `type:`）到构造函数的映射，是一张全局 map：

```go
// pkg/epp/framework/interface/plugin/registry.go:26-72（节选）
// FactoryFunc is the definition of the factory functions that are used to instantiate plugins
// specified in a configuration. The framework provides a strict decoder
// (DisallowUnknownFields) over the plugin's raw parameters...
type FactoryFunc func(name string, parameters *json.Decoder, handle Handle) (Plugin, error)

// Register registers a factory function for the given plugin type along with its stability level.
func Register(pluginType string, stability StabilityLevel, factory FactoryFunc) {
	Registry[pluginType] = factory
	RegistryMetadata[pluginType] = PluginMetadata{
		Type:      pluginType,
		Stability: stability,
	}
}

// RegisterAsDefaultProducer registers a factory ... and records it as the default producer for the given data key.
func RegisterAsDefaultProducer(pluginType string, stability StabilityLevel, factory FactoryFunc, key DataKey) {
	Register(pluginType, stability, factory)
	DefaultProducerRegistry[key.String()] = pluginType
}

// RegisterWithPluginDependencies registers a factory ... and records it as dependent on other plugins
// referenced in the configuration struct returned by the plugin's configuration parser function.
func RegisterWithPluginDependencies(pluginType string, stability StabilityLevel, factory FactoryFunc, parser ConfigParserFunc) {
	Register(pluginType, stability, factory)
	PluginsWithPluginDependencies[pluginType] = parser
}
```

四种注册方式的分工：

| 函数 | 额外登记了什么 | 用于 |
|------|---------------|------|
| `Register` | 只有类型和稳定性 | 普通插件 |
| `RegisterAsDefaultProducer` | 「谁是这个 DataKey 的默认生产者」 | 自动补齐缺失的 producer（§4.2） |
| `RegisterWithPluginDependencies` | 参数解析函数（用于扫 `pluginRef`） | 插件参数里引用其他插件的情况 |
| `RegisterDeprecated`（`registry.go:75`） | 弃用版本 + 替代类型 | 启动时打 DEPRECATION 日志 |

**所有内建插件的注册集中在一个函数**：`registerInTreePlugins()`（`cmd/epp/runner/runner.go:573-736`），在 Phase One 被调用（`runner.go:764`）。想知道「这个版本到底有哪些插件」，读这 160 行就够了。

### 2.2 严格解析：未知字段直接报错

`StrictDecoder`（`registry.go:41-48`）对插件参数开了 `DisallowUnknownFields`。这意味着**参数名打错一个字母，EPP 启动就失败**，而不是静默忽略：

```yaml
- type: prefix-cache-scorer
  parameters:
    prefixMatchInfoProducerNam: precise   # 少了个 e
```

→ `failed to create plugin 'prefix-cache-scorer' (type: prefix-cache-scorer): json: unknown field "prefixMatchInfoProducerNam"`

这是好事。作为对比，**有一批配置项没有这层保护、配错了会静默不生效**，汇总在 [07 篇 §3](07-部署示例与端到端Demo.md#3-静默失效模式总表)。

### 2.3 实例化：按依赖 DAG 拓扑序

```go
// pkg/epp/config/loader/configloader.go:276-298
func instantiatePlugins(configuredPlugins []configapi.PluginSpec, handle fwkplugin.Handle, logger logr.Logger) error {
	orderedPlugins, err := buildPluginDAG(configuredPlugins, handle)
	if err != nil {
		return fmt.Errorf("failed to build plugin dependency graph: %w", err)
	}

	for _, spec := range orderedPlugins {

		if meta, ok := fwkplugin.RegistryMetadata[spec.Type]; ok && meta.Deprecated {
			logger.Info("DEPRECATION warning: plugin is deprecated", "plugin", spec.Name, "type", spec.Type)
		}

		factory := fwkplugin.Registry[spec.Type]
		plugin, err := factory(spec.Name, fwkplugin.StrictDecoder(spec.Parameters), handle)
		if err != nil {
			return fmt.Errorf("failed to create plugin '%s' (type: %s): %w", spec.Name, spec.Type, err)
		}

		handle.AddPlugin(spec.Name, plugin)
	}

	return nil
}
```

`buildPluginDAG`（`configloader.go:300-334`）用**反射**扫插件配置结构体上的 `pluginRef` tag，做拓扑排序。为什么需要：`prefix-cache-scorer` 的参数里写着 `prefixMatchInfoProducerName: precise`，它构造时需要那个 producer 实例已经存在。

### 2.4 完整链路

```
EndpointPickerConfig（YAML / --config-text）
  │
  ├─ registerInTreePlugins()                        runner.go:573-736
  │    └─ 必须最先跑：Registry 里没有该 type 就无从校验、更无从实例化
  │
  ├─[Phase One] LoadRawConfig                       configloader.go:82-156
  │    ├─ 解码 + applyStaticDefaults
  │    ├─ 合并 featureGates（CLI 追加在后，last-wins）
  │    └─ validateFeatureGates（**未知 gate** 在这里被抓；未知 type 在下面）
  │
  ├─[Phase Two] InstantiateAndConfigure             configloader.go:179-251
  │    ├─ validatePlugins（**未知 type 在这里才被抓**）    configloader.go:184
  │    ├─ instantiatePlugins（DAG 拓扑序 → Registry[type] → factory）
  │    ├─ applySystemDefaults（注入缺失的 picker / handler / parser / metrics …）
  │    ├─ buildSchedulerConfig（WeightedScorer 包装 + profiles map）
  │    └─ buildDataLayerConfig / buildParserRegistry / buildFlowControlConfig
  │
  └─ 回到 runner，在 InstantiateAndConfigure **返回之后**：
       ├─ CreateMissingDataProducers（DefaultProducerRegistry）   runner.go:807
       └─ OrderPlugins（按数据依赖排 requestControl 执行序）       runner.go:833
```

### 2.5 两阶段解析与启动装配

上面那条链路为什么要拆成两个 phase？因为**注册与实例化有前后依赖，而实例化又依赖 datastore 已就绪**：

| 阶段 | 函数 | 做什么 | 为什么必须单独一趟 |
|------|------|--------|------------------|
| Phase One | `parseConfigurationPhaseOne`（`runner.go:738-776`） | 读配置文本、**注册所有 in-tree 插件**、合并 feature gates | 插件类型必须先在 `Registry` 里存在才能实例化；而且要先知道用 file discovery 还是 K8s 模式 |
| Phase Two | `parseConfigurationPhaseTwo`（`runner.go:792-841`） | 按依赖 DAG **实例化**插件、注入默认插件、组装 scheduler | 插件的构造函数要能列 pod，所以必须等 `datastore` 就绪 |

`epp` 的 `main` 本身只有几行，全部装配在 `cmd/epp/runner/runner.go` 的 `Run()`（`runner.go:213`）里：

```go
// cmd/epp/main.go:35-49（节选）
func run() int {
	ctx := ctrl.SetupSignalHandler()

	// Note: GIE built-in plugins are automatically registered by the runner
	// when it processes configuration in runner.parsePluginsConfiguration()

	if err := runner.NewRunner().Run(ctx); err != nil {
		return 1
	}
	return 0
}
```

Phase Two 的主体：

```go
// cmd/epp/runner/runner.go:792-841（节选）
func (r *Runner) parseConfigurationPhaseTwo(ctx context.Context, rawConfig *configapi.EndpointPickerConfig, ds datastore.Datastore) (*config.Config, error) {
	handle := fwkplugin.NewEppHandle(ctx, makePodListFunc(ds), fwkplugin.WithMetricsRecorder(ctrlmetrics.Registry))
	cfg, err := loader.InstantiateAndConfigure(rawConfig, handle, logger)
	// ...
	r.schedulerConfig = cfg.SchedulerConfig
	// Auto-create missing DataProducers...
	r.requestControlConfig.AddPlugins(handle.GetAllPlugins()...)
	// RegisterDependencies → ValidateAndOrderDataDependencies → OrderPlugins
	r.parserRegistry = cfg.ParserRegistry
	return cfg, nil
}
```

**`makePodListFunc(ds)` 这个闭包是关键**：插件在**构造时**就拿到了「列出当前所有 pod」的能力，所以像 `inflight-load-producer` 这类需要跟踪 endpoint 生命周期的插件可以在构造期就注册好 watch，不用等到第一个请求进来。

启动期的 flag 与优雅退出行为见 [01 篇 §1.3](01-请求的一生-主控制流.md#13-关键-cli-flag)。

## 3. 打分：数字是怎么出来的

这一节回答 01 篇示例里「为什么选 D3」。

### 3.1 值域约定与 clamp

`Scorer` 接口注释**规定**返回值必须在 `[0,1]`，但那只是一条约定；真正生效的截断只有一处——框架不信任插件，在累加时自己 clamp（`enforceScoreRange`，`scheduler_profile.go:427-435`，全仓库只在 `scheduler_profile.go:254` 被调用一次）。所以插件返回 1.5 不会报错，会被当成 1.0。

### 3.2 权重包装

配置解析时，每个 scorer 被包一层 `WeightedScorer`：

```go
// pkg/epp/config/loader/configloader.go:398-405
			// Wrap Scorers with weights.
			if scorer, ok := plugin.(fwksched.Scorer); ok {
				weight := DefaultScorerWeight
				if pluginRef.Weight != nil {
					weight = *pluginRef.Weight
				}
				plugin = scheduling.NewWeightedScorer(scorer, weight)
			}
```

`DefaultScorerWeight` = `1.0`（`defaults.go:42-43`）。

### 3.3 累加，但不归一化

```go
// pkg/epp/scheduling/scheduler_profile.go:244-255（节选）
	// Iterate through each scorer in the chain and accumulate the weighted scores.
	for _, scorer := range p.scorers {
		scores := runScorer(ctx, tracer, tracingActive, scorer, request, endpoints)
		for endpoint, score := range scores { // weight is relative to the sum of weights
			weightedScorePerEndpoint[endpoint] += enforceScoreRange(score) * scorer.Weight()
		}
	}
```

**三个必须记住的事实**：

1. **公式就是 `Σ(scoreᵢ × weightᵢ)`**，没有除以 `Σweightᵢ`。那句 `// weight is relative to the sum of weights` 是语义描述而非代码行为。
2. **scorer 串行执行**，不是并行。`for _, scorer := range p.scorers` 单 goroutine 走完。上游架构文档写着「future: parallel execution」。
3. **最终分值的绝对尺度随权重配置变化**。optimized-baseline 的权重是 2+2+3+2=9，所以理论最高分是 9 分而不是 1 分。跨配置比较分数没有意义。

### 3.4 用示例算一遍

场景是统一示例（00 篇 §2）：D1..D4 四个 decode pod，近似前缀路线（block size 64），配置用 optimized-baseline 的权重 `queue×2 + kv×2 + prefix×3 + lru×2`。请求 A 到达时采到的状态：

| pod | 等待队列 | KV 利用率 |
|-----|---------|----------|
| D1 | 8 | 0.62 |
| D2 | 5 | 0.55 |
| D3 | **2** | **0.30** |
| D4 | 12 | 0.71 |

各 scorer 的算法：

- **`queue-scorer`**：候选集内 min-max 归一化，`(max-q)/(max-min)`，全相等时给 1.0（`queuedepth/queue.go:100-106`）
- **`kv-cache-utilization-scorer`**：直接 `1 - KVCacheUsagePercent`
- **`prefix-cache-scorer`**：默认只用 match ratio = `matchBlocks/totalBlocks`（04 篇 §5）
- **`no-hit-lru-scorer`**：分散冷请求，但它有个**很容易想错的开关**，见下

**先把 `no-hit-lru-scorer` 的两段行为讲清楚**，否则下面的算式看不懂（`nohitlru/no_hit_lru.go:287-303`）：

```go
	isCold := s.isColdRequest(ctx, endpoints)
	// ...
	if !isCold {
		logger.Info("Cache hit detected, returning neutral scores")
		return s.scoreNeutral(endpoints)      // 全部 0.5
	}
	return s.scoreColdRequestByLRU(endpoints)
```

- **`isColdRequest` 只要发现任何一个 endpoint 有前缀命中就返回 false**（`:171-183`，`info.MatchBlocks() > 0` 即 return false）。此时整轮走 `scoreNeutral`，**四个 pod 一律 0.5**——它主动让路给前缀亲和，不参与竞争。
- 冷请求（全都没命中）时才按 LRU 排序。而且从未被选过的 pod **不是全给 1.0**，是按候选顺序递减 `1.0 - i/(n-1)`（`scoreNeverUsedPods`，`:226-235`）。

**请求 A**——2088 token，切成 32 个完整 block + 1 个残块 = 33 块，索引里全空。前缀分全 0，是个**冷请求**：

```
queue-scorer:  min=2, max=12
  D1: (12-8)/(12-2)=0.400  D2: (12-5)/10=0.700  D3: (12-2)/10=1.000  D4: 0.000
kv-scorer:     1 - KV
  D1: 0.38                 D2: 0.45             D3: 0.70             D4: 0.29
prefix-scorer: 全 0（首次请求，没人有这个前缀）
no-hit-lru:    冷请求 + 四个都没用过 → 按候选顺序 1.0 / 0.667 / 0.333 / 0.0

加权和（queue×2 + kv×2 + prefix×3 + lru×2）：
  D1: 0.80 + 0.76 + 0 + 2.00  = 3.56
  D2: 1.40 + 0.90 + 0 + 1.33  = 3.63
  D3: 2.00 + 1.40 + 0 + 0.67  = 4.07   ← max-score-picker 选 D3
  D4: 0.00 + 0.58 + 0 + 0.00  = 0.58
```

**请求 B**——A 已经落在 D3 并跑起来了（D3 队列变 3、KV 变 0.35），B 共 2072 token = 32 个完整 block + 1 个残块，**与 A 共享的 2048 token 正好就是那 32 块**，所以 D3 的 match ratio 是 `32/33 = 0.970`。**关键变化：D3 有命中，于是整个请求不再是冷请求**：

```
queue-scorer:  min=3, max=12
  D1: (12-8)/9=0.444       D2: (12-5)/9=0.778   D3: (12-3)/9=1.000   D4: 0.000
kv-scorer:
  D1: 0.38                 D2: 0.45             D3: 0.65             D4: 0.29
prefix-scorer: D3: 32/33=0.970，其余 0
no-hit-lru:    检测到命中 → 整轮中性，四个一律 0.5（不是"D3 被扣分"）

加权和：
  D1: 0.89 + 0.76 + 0.00 + 1.00 = 2.65
  D2: 1.56 + 0.90 + 0.00 + 1.00 = 3.46
  D3: 2.00 + 1.30 + 2.91 + 1.00 = 7.21   ← 仍然选 D3，领先 3.75 分
  D4: 0.00 + 0.58 + 0.00 + 1.00 = 1.58
```

**这里最值得记的不是数字，而是 Affinity 与 Distribution 并没有"正面对撞"**：`no-hit-lru-scorer` 一检测到命中就整轮弃权，让前缀亲和无阻力地拿下那 2.91 分。这是**刻意的设计**——分散策略只在"反正谁都没缓存"的冷请求上起作用，一旦有缓存可复用就不该跟前缀亲和抢。所以 `prefix-cache-scorer` 在官方配方里取最高权重 3，并不是靠权重"压过"了分散策略，而是因为**在有命中的场合它是唯一有意见的 scorer**（除了负载类）。

**反过来说，真正会和前缀亲和竞争的是 queue 与 kv 这两个负载 scorer**：D3 若队列极长，`queue-scorer` 给它 0 分、少 2 分，而前缀满命中最多给 3 分，仍然赢——这才是调权重时要权衡的那一对。

**注意这套算式成立的前提**：共享前缀有 2048 token，跨过了 32 个完整 block。如果系统提示只有十几个 token，`prefix-scorer` 那一列会**全是 0**，请求退化成冷请求、由 LRU 分散推走，前缀缓存路由完全不生效且没有任何报错。边界条件见 04 篇 §4.2。

### 3.5 平局怎么办

`max-score-picker`（`picker/maxscore/picker.go`）先做确定性轮转，再稳定排序：

```
RotateScoredEndpoints（picker.go:91-93）  → 轮转起点，等分时轮流中选
stable sort by score desc（picker.go:95-103）
```

参数 `maxNumOfEndpoints`（默认 1）控制返回几个 endpoint。返回多个时 Envoy 的 destination 是逗号分隔列表——用于 data parallel 等场景。

## 4. 默认注入：你没配的东西

### 4.1 系统默认

```go
// pkg/epp/config/loader/defaults.go:124-141（节选）
func applySystemDefaults(cfg *configapi.EndpointPickerConfig, handle fwkplugin.Handle) error {
	if err := ensureSchedulingLayer(cfg, handle, allPlugins); err != nil { /* ... */ }
	if err := ensureFlowControlLayer(cfg, handle, allPlugins); err != nil { /* ... */ }
	if err := ensureParsers(cfg, handle, allPlugins); err != nil { /* ... */ }
	if err := ensureSaturationDetector(cfg, handle, allPlugins); err != nil { /* ... */ }
	if err := ensureDataLayer(cfg, handle, allPlugins); err != nil { /* ... */ }
	return nil
}
```

完整的默认值表：

| 你没配 | 框架注入 |
|--------|---------|
| `schedulingProfiles` 整段 | 自动建一个名为 `default` 的 profile，把所有已实例化的 Filter/Scorer/Picker 塞进去 |
| ProfileHandler（且只有 1 个 profile） | `single-profile-handler` |
| profile 里的 Picker | 复用已有的第一个 Picker，没有则注入 `max-score-picker` |
| Scorer 的 `weight` | `1.0` |
| Parser | `openai-parser`、`anthropic-parser`、`vllmhttp-parser`、`passthrough-parser`（**顺序有意义**，先匹配的先用） |
| Flow Control 的排序/公平/限额策略 | `fcfs-ordering-policy`、`global-strict-fairness-policy`、`static-usage-limit-policy` |
| `saturationDetector` | `utilization-detector` |
| Data Layer 的 source/extractor | `metrics-data-source` + `core-metrics-extractor`（除非 `dataLayer.injectDefaults: false`，或已显式配了 `metrics-data-source`） |
| **整个配置文件为空** | `loadDefaultConfig()`（`defaults.go:47-104`）：queue/kv/prefix 三个 scorer（权重 2/2/3），**外加 `metrics-data-source`（`:68`）、`core-metrics-extractor`（`:71`）和一整段 `dataLayer.sources`（`:93-102`）**——默认配置是自带数据源的 |

最后一条很实用：**不给 `--config-file` 也能跑**，得到的就是一套「负载 + 前缀」的合理默认。

### 4.2 DataProducer 自动补齐

这一步不在 `defaults.go`，而在 Phase Two 之后（`runner.go:807` 调 `CreateMissingDataProducers`，实现在 `data_graph.go:65-72`）。

机制：插件用 `Consumes()` 声明需要某个 `DataKey`，如果没有任何插件 `Produces()` 它，就从 `DefaultProducerRegistry` 找默认生产者建一个。

| 需要的数据 | 默认 producer |
|-----------|--------------|
| `PrefixCacheMatchInfo` | `approx-prefix-cache-producer` |
| `TokenizedPrompt` | `token-producer` |
| `InFlightLoad` / `UncachedRequestTokens` | `inflight-load-producer` |
| `LatencyPredictionInfo` | `predicted-latency-producer` |
| 多模态 embedding 缓存 | `mm-embeddings-cache-producer` |
| `SessionID` | `session-id-producer` |

注册见 `runner.go:668-677`。

**这解释了 optimized-baseline 配置里的一个现象**：它显式配了 `approx-prefix-cache-producer`，但如果只写 `prefix-cache-scorer` 不写 producer，也能工作——框架会自动补一个近似 producer。**想用精确版本必须显式配**，否则默默用了近似的。

## 5. 内置插件清单

以下按扩展点分组。稳定性以 `registerInTreePlugins` 为准，Alpha 插件需 `--allow-experimental-plugins`，否则**启动失败**（`registry.go:94-107` 的 `ValidatePluginStability`）。

**表里标了 `(Alpha)` 的都要开那个 flag**。除此之外还有 `disaggregatedset-rollout-screener`（`runner.go:577`）、`burst-prefix-cache-producer`（`:644`）、`multicluster-file-discovery`（`:730`）、`multicluster-metrics-data-source` / `multicluster-metrics-extractor`（`:700-701`）也是 Alpha。想确认某个插件的稳定性，最快的办法是 grep 它的注册行看第二个参数是 `StabilityAlpha` 还是 `StabilityBeta`。

### 5.1 Filter

| YAML type | 文件（`framework/plugins/scheduling/filter/`） | 作用 | 主要参数 |
|-----------|---------------------------------------------|------|---------|
| `decode-filter` | `bylabel/roles.go:28` | 只留 `role=decode` 的 pod | — |
| `prefill-filter` | `bylabel/roles.go:30` | 只留 `role=prefill` | — |
| `encode-filter` | `bylabel/roles.go:32` | 只留 `role=encode` | — |
| `label-selector-filter` | `bylabel/selector.go` | 通用 label 过滤 | **无包装字段**，参数体直接是 `metav1.LabelSelector`：写 `matchLabels:` / `matchExpressions:` |
| `utilization-filter` **(Alpha)** | `utilization/filter.go` | 按指标硬剔除过载 pod | `conditions[]`（`metric`/`maxValue`）、`fallbackOnEmpty`、`inFlightLoadProducerName` |
| `session-affinity-filter` **(Alpha)** | `sessionaffinity/session_affinity.go` | 会话亲和**硬**过滤 | `strategy`、`sessionIdConfig`、`profileName` |
| `prefix-cache-affinity-filter` | `prefixcacheaffinity/plugin.go` | 前缀亲和硬过滤 | — |
| `slo-headroom-tier-filter` | `sloheadroomtier/plugin.go` | 按 SLO 余量分层 | — |
| `endpoint-attribute-filter` **(Alpha)** | `endpointattribute/filter.go` | 按 endpoint 属性过滤 | — |
| `topology-affinity-filter` **(Alpha)** | `topologyaffinity/filter.go` | 拓扑亲和 | — |
| `multicluster-session-affinity-filter` **(Alpha)** | `sessionaffinity/multicluster.go` | 多集群变体 | 同 session-affinity |

已弃用：`by-label`、`by-label-selector` → 用 `label-selector-filter`。

`utilization-filter` 值得注意——它的 `fallbackOnEmpty` 参数决定「所有 pod 都超阈值时怎么办」：不开则返回空集触发 429，开了则退回全集。**生产环境通常要开**，否则一次流量尖峰会让整个池子返回 429。**但它本身是 Alpha**（`runner.go:592`），用它之前得先加 `--allow-experimental-plugins`，否则 EPP 直接起不来。

### 5.2 Scorer

| YAML type | 文件（`.../scorer/`） | 打分依据 | Category |
|-----------|---------------------|---------|----------|
| `queue-scorer` | `queuedepth/queue.go` | 等待队列，候选集内 min-max 归一化 | Distribution |
| `kv-cache-utilization-scorer` | `kvcacheutilization/kvcache_utilization.go` | `1 - KVCacheUsagePercent` | Distribution |
| `prefix-cache-scorer` | `prefix/plugin.go` | 前缀匹配（近似或精确，取决于绑的 producer） | Affinity |
| `precise-prefix-cache-scorer` | `preciseprefixcache/precise_prefix_cache.go` | **已弃用**，见 §5.2.1 | Affinity |
| `load-aware-scorer` | `loadaware/load_aware.go` | 综合负载 | Distribution（`load_aware.go:73-75`） |
| `session-affinity-scorer` **(Alpha)** | `sessionaffinity/session_affinity.go` | 会话亲和**软**打分 | Affinity |
| `lora-affinity-scorer` | `loraaffinity/lora_affinity.go` | LoRA adapter 已加载在哪 | Affinity |
| `latency-scorer` | `latency/plugin.go` | 预测的 TTFT/ITL（配合 latency-predictor） | Balance |
| `running-requests-size-scorer` | `runningrequests/runningrequest.go` | 运行中请求数 | Distribution |
| `token-load-scorer` | `tokenload/token_load.go` | token 维度的负载 | Distribution |
| `no-hit-lru-scorer` | `nohitlru/no_hit_lru.go` | 冷请求（无前缀命中）时分散 | Distribution |
| `active-request-scorer` | `activerequest/active_request.go` | 活跃请求数 | Distribution |
| `context-length-aware` | `contextlengthaware/` | 上下文长度 | — |
| `mm-embeddings-cache-scorer` | `mmcacheaffinity/` | 多模态 embedding 缓存亲和 | Affinity |
| `header-label-affinity-scorer` **(Alpha)** | `headerlabelaffinity/` | header → label 亲和 | Affinity |
| `endpoint-attribute-scorer` **(Alpha)** | `endpointattribute/` | 通用属性打分 | — |
| `topology-affinity-scorer` **(Alpha)** | `topologyaffinity/` | 拓扑亲和 | Affinity |
| `multicluster-*`（queue / kv / prefix） **(全部 Alpha)** | 各自的 `multicluster.go` | 多集群变体 | — |

#### 5.2.1 两个必须知道的 scorer 缺陷

**`precise-prefix-cache-scorer` 已弃用**（`precise_prefix_cache.go:87-89` 启动即打 DEPRECATION 日志）。正确写法是 `precise-prefix-cache-producer` + `prefix-cache-scorer`（后者设 `prefixMatchInfoProducerName` 指向前者）。04 篇 §5 给完整配置。

**`load-aware-scorer` 信息不足**，注释直接承认：

```go
// pkg/epp/framework/plugins/scheduling/scorer/loadaware/load_aware.go:77-80（节选）
// Currently metrics contains number of requests waiting in the queue, there is no information about
// number of requests that can be processed... Pod with empty waiting requests queue is scored with 0.5
```

**后果**：空闲的 pod（队列为 0）全部得 0.5，彼此**无法区分**。而 03 篇会讲到，指标抓取失败时 `WaitingQueueSize` 也是 0——所以「pod 全空闲」和「指标全挂了」在这个 scorer 眼里一模一样。生产环境用 `queue-scorer` + `kv-cache-utilization-scorer` 比用 `load-aware-scorer` 更可控。

**三件套依赖只适用于两个指标类 scorer**（`plugins/README.md:36`）：`multicluster-kv-cache-utilization-scorer` 与 `multicluster-queue-scorer` 必须同时配 `multicluster-metrics-data-source` + `multicluster-metrics-extractor`，否则加载失败。而 `multicluster-prefix-cache-scorer` / `multicluster-session-affinity-filter` / `multicluster-approx-prefix-cache-producer` 是 README 第 32 行说的「pure-delegation variants」——只换了 type 名、复用原插件行为，**不吃这个依赖**。

### 5.3 Picker

| YAML type | 文件 | 算法 | 参数 |
|-----------|------|------|------|
| `max-score-picker` | `picker/maxscore/picker.go` | 最高分；平局确定性轮转 | `maxNumOfEndpoints`（默认 1） |
| `random-picker` | `picker/random/picker.go` | 纯随机 | `maxNumOfEndpoints` |
| `weighted-random-picker` | `picker/weightedrandom/picker.go` | 按分数加权随机（A-Res 算法） | `maxNumOfEndpoints` |

`weighted-random-picker` 的用途：`max-score-picker` 会把所有相似请求都打到同一个最优 pod，造成羊群效应；加权随机在保留偏好的同时引入抖动。

### 5.4 ProfileHandler 与 PD Decider

| YAML type | 文件（`.../profilehandler/`） | 作用 |
|-----------|---------------------------|------|
| `single-profile-handler` | `single/single_profile_handler.go` | 单 profile，恒为 primary |
| `disagg-profile-handler` | `disagg/disagg_profile_handler.go` | **P/D/E 分阶段调度**（见下） |
| `header-profile-handler` | `headerprofile/` | 按请求头选 profile（Coordinator 模式用，06 篇 §7） |
| `data-parallel-profile-handler` | `dataparallel/` | DP 多 rank |

`disagg-profile-handler` 的参数结构（`disagg_profile_handler.go:87-97`）：

```yaml
- type: disagg-profile-handler
  parameters:
    stageOrder: decode-first        # 或 prefill-first
    profiles:
      decode: decode
      prefill: prefill
    deciders:
      prefill: prefix-based-pd-decider   # pluginRef
```

**Decider** 决定「这个请求要不要真的走 prefill 分离」：

| YAML type | 文件 | 逻辑 |
|-----------|------|------|
| `always-disagg-pd-decider` | `disagg/always_disagg_pd_decider.go` | 总是分离 |
| `prefix-based-pd-decider` | `disagg/prefix_based_pd_decider.go` | 未命中的 token 数超过 `nonCachedTokens` 才分离 |
| `always-disagg-multimodal-decider` | `disagg/always_disagg_mm_decider.go` | 多模态总是分离 |

`prefix-based-pd-decider` 是生产配方里的默认选择（`deploy/config/pd-epp-config.yaml` 设 `nonCachedTokens: 16`）。**道理**：prompt 短或前缀基本全命中时，分离的网络开销大于 prefill 的计算收益，不如让 decode pod 自己算。

**两个坑**：

1. `single-profile-handler` **不能**用于多 profile，会启动失败（`configloader.go:429-431`）
2. 全局只能有一个 ProfileHandler，配了两个也启动失败（`configloader.go:414-423`）

已弃用：`disagg-headers-handler`、`prefill-header-handler` → 用 `disagg-profile-handler`。

### 5.5 DataProducer

| YAML type | 文件（`.../requestcontrol/dataproducer/`） | 产出 |
|-----------|----------------------------------------|------|
| `token-producer` | `tokenizer/tokenizer.go` | 分词结果（token IDs、多模态特征） |
| `approx-prefix-cache-producer` | `approximateprefix/plugin.go` | 近似前缀匹配（EPP 自己的路由历史 LRU） |
| `precise-prefix-cache-producer` | `preciseprefixcache/producer.go` | 精确前缀匹配（vLLM 真实 KV 事件） |
| `inflight-load-producer` | `inflightload/producer.go` | EPP 侧统计的在途负载 |
| `predicted-latency-producer` | `predictedlatency/` | 延迟预测输入与 SLO 上下文 |
| `session-id-producer` | `sessionid/producer.go` | 从 header/cookie 提取 session ID |
| `mm-embeddings-cache-producer` | `multimodal/producer.go` | 多模态 embedding 缓存匹配 |
| `burst-prefix-cache-producer` | `burstprefix/` | Alpha |
| `p2p-source-producer` | `p2psource/` | Alpha |
| `multicluster-approx-prefix-cache-producer` | `approximateprefix/multicluster.go` | 近似前缀的多集群变体（Alpha） |
| ~~`tokenizer`~~ | `tokenizer/` | **已弃用**（`runner.go:674` 用 `RegisterDeprecated` 注册，替代者 `token-producer`），配了会打 DEPRECATION 日志 |

`token-producer` 的 `vllm` 后端会调 vLLM 的 tokenize 接口（准确但有网络开销），`estimate` 后端本地估算（快但不准）。

### 5.6 Admitter / Screener / RequestHeader

| YAML type | 扩展点 | 文件 | 作用 |
|-----------|-------|------|------|
| `latency-slo-admitter` | Admission | `admitter/latencyslo/` | 预测延迟超 SLO 就拒 |
| `probabilistic-admitter` | Admission | `admitter/probabilisticadmitter/` | 概率性拒绝（限流） |
| `disaggregatedset-rollout-screener` | Screener | `screener/disaggregatedsetrollout/` | P/D 分片灰度时的候选筛选 |
| `agent-identity` | RequestHeader | `requestheader/agentidentity/` | 提取调用方身份 → FairnessID（Alpha） |
| `outlen-bucket` | RequestHeader | `requestheader/outlenbucket/` | 输出长度分桶（Alpha） |
| `request-attribute-reporter` | 响应侧 | `requestcontrol/reporter/` | 把请求属性写进指标/日志 |

`agent-identity` 与 05 篇的 Flow Control 直接相关：它产出的身份决定了请求进哪个公平性队列。

### 5.7 Data Layer 插件

| YAML type | 文件（`.../datalayer/`） | 作用 |
|-----------|----------------------|------|
| `metrics-data-source` | `source/metrics/datasource.go` | 抓 `/metrics` |
| `core-metrics-extractor` | `extractor/metrics/extractor.go` | 解析队列/KV/运行数等 |
| `models-data-source` / `models-data-extractor` | `source/models`、`extractor/models` | 抓 `/v1/models`，发现 served model 与 LoRA |
| `dcgm-data-source` / `dcgm-extractor` | `source/dcgm`、`extractor/dcgm` | GPU 硬件指标（Alpha） |
| `topology-extractor` | `extractor/topology/` | 拓扑属性（Alpha） |
| `k8s-notification-source` | `source/notifications/` | K8s 对象变更事件驱动 |
| `endpoint-notification-source` | `source/notifications/endpoint_datasource.go` | endpoint 上下线事件 |
| `file-discovery` | `discovery/file/plugin.go` | 从文件读 endpoint 列表（无 K8s 模式） |
| `multicluster-file-discovery` | `discovery/file/` | 多集群变体（Alpha） |
| `multicluster-metrics-data-source` / `multicluster-metrics-extractor` | `cross_plugin/multicluster/` | 多集群指标三件套里的两件（Alpha，见 §5.2.1） |
| ~~`local-syncer`~~ | `cross_plugin/local/` | 跨 EPP 副本状态同步。**注意它没有注册进 `registerInTreePlugins()`**，只有测试里注册（`run_with_file_discovery_test.go:50`）。生产配置里写 `type: local-syncer` 会被 `validatePlugins` 拒绝：`plugin type 'local-syncer' is not registered` |

`metrics-data-source` 有个**很容易踩的坑**（03 篇会再提）：

```go
// pkg/epp/framework/plugins/datalayer/source/metrics/datasource.go:47-51（节选）
// 单一 port 参数会覆盖所有 endpoint。
```

DP 场景下一个 pod 有多个 target port（每个 rank 一个），如果显式设了 `port`，所有 rank 都会去抓同一个端口——**指标全错但不报错**。多端口池子不要设 `port`。

### 5.8 Parser

| YAML type | 文件 | 解析什么 |
|-----------|------|---------|
| `openai-parser` | `parsers/openai/openai.go` | `/v1/chat/completions`、`/v1/completions` |
| `anthropic-parser` | `parsers/anthropic/` | `/v1/messages` |
| `vllmhttp-parser` / `vllmgrpc-parser` | `parsers/vllmhttp`、`parsers/vllmgrpc` | vLLM 原生接口 |
| `sglanghttp-parser` | `parsers/sglanghttp/` | SGLang 原生 `/generate` |
| `vertexai-parser` | `parsers/vertexai/` | Vertex AI 格式 |
| `passthrough-parser` | `parsers/passthrough/` | 兜底，不解析 |

**顺序有意义**：Parser 靠 `Claims()` 声明自己能处理哪些路径/格式，框架按注册顺序匹配。`passthrough-parser` 必须在最后。

### 5.9 Flow Control 策略

完整清单与算法见 [05 篇](05-核心代码分析-FlowControl流控与准入.md)，这里只列 type 名：

| 类别 | 插件 |
|------|------|
| 公平性 | `global-strict-fairness-policy`（默认）、`round-robin-fairness-policy`、`program-aware-fairness` |
| 排序 | `fcfs-ordering-policy`（默认）、`edf-ordering-policy`、`slo-deadline-ordering-policy` |
| 用量限制 | `static-usage-limit-policy`（默认）、`priority-holdback-policy`、`soft-reflective-ceiling-policy` |
| 饱和检测 | `utilization-detector`（默认）、`concurrency-detector` |
| 驱逐 | `sheddable-eviction-filter`、`priority-then-time-eviction-order-policy` |

## 6. SchedulingProfiles

### 6.1 API 结构

```go
// apix/config/v1alpha1/endpointpickerconfig_types.go:112-179（节选）
type PluginSpec struct {
	Name       string          `json:"name"`
	Type       string          `json:"type"`
	Parameters json.RawMessage `json:"parameters"`
}

type SchedulingProfile struct {
	Name    string             `json:"name"`
	Plugins []SchedulingPlugin `json:"plugins"`
}

type SchedulingPlugin struct {
	PluginRef string   `json:"pluginRef"`
	Weight    *float64 `json:"weight"`
}
```

`name` 省略时默认等于 `type`（`defaults.go:110-113`）。这就是为什么大多数配置里的 `pluginRef` 直接写 type 名——**同一个 type 要配多个实例时才需要显式命名**，比如两个不同 block size 的前缀 producer。

### 6.2 单 profile vs 多 profile

| | 单 profile | 多 profile（P/D） |
|---|-----------|------------------|
| ProfileHandler | 自动注入 `single-profile-handler` | 必须显式配 `disagg-profile-handler` |
| Pick | 始终返回那一个 profile | 按 stageOrder + decider 迭代 |
| Filter 链 | 一条 | **每个 profile 各一条独立的** |
| Screener | Director 层跑一次，对所有 profile 生效 | 同左 |
| primary | 那唯一一个 | `decode`（默认） |

**Filter 链独立**这点是 P/D 配置的核心：`prefill` profile 里配 `prefill-filter`，`decode` profile 里配 `decode-filter`，两条链互不干扰。而 `prefix-cache-scorer` 可以在两个 profile 里都出现——同一个插件实例，被两条链各调一次。

### 6.3 校验

`buildSchedulerConfig`（`configloader.go:380-434`）在启动时抓这几类错误：

```go
// pkg/epp/config/loader/configloader.go:414-431（节选）
	var profileHandler fwksched.ProfileHandler
	for name, plugin := range handle.GetAllPluginsWithNames() {
		if ph, ok := plugin.(fwksched.ProfileHandler); ok {
			if profileHandler != nil {
				return nil, fmt.Errorf("multiple profile handlers found ('%s', '%s'); only one is allowed",
					profileHandler.TypedName().Name, name)
			}
			profileHandler = ph
		}
	}

	if profileHandler == nil {
		return nil, errors.New("no profile handler configured")
	}

	if profileHandler.TypedName().Type == single.SingleProfileHandlerType && len(profiles) > 1 {
		return nil, errors.New("SingleProfileHandler cannot support multiple scheduling profiles")
	}
```

再加上 `pluginRef` 指向不存在的插件（`configloader.go:394-396`）。**这些都是启动期硬失败**——比运行时静默降级好得多。

## 7. Feature Gates

### 7.1 只有两个

| Gate | 默认 | 作用 | 定义 |
|------|------|------|------|
| `flowControl` | **false** | 启用整个 Flow Control 层 | `flowcontrol/config.go:27` |
| `haPopulateNonLeaderDatastore` | **true** | HA 部署下非 leader 副本也跑 datastore reconciler | `server/controller_config.go:33` |

注册见 `runner.go:761-762`。

### 7.2 CLI 覆盖 YAML

```go
// pkg/epp/config/loader/configloader.go:135-142
	// Appended after the config's own entries because loadFeatureConfig is last-wins,
	// so flag-supplied gates override file-supplied ones.
	if len(extraGates) > 0 {
		rawConfig.FeatureGates = append(rawConfig.FeatureGates, extraGates...)
	}
```

`loadFeatureConfig`（`configloader.go:436-455`）先填所有已注册 gate 的默认值，再按列表顺序解析，同名后者覆盖前者。格式是 kubelet 风格：`gateName`（= true）或 `gateName=false`。

未知 gate 名会在校验期报错（`loader/validation.go:138+`）。

### 7.3 gate 关闭时配了对应段会怎样

```go
// pkg/epp/config/loader/configloader.go:219-228（节选）
	if featureGates[flowcontrol.FeatureGate] {
		flowControlConfig, err = buildFlowControlConfig(...)
	} else if flowControlSettingsConfigured(rawConfig.FlowControl) {
		logger.Info("WARNING: the flowControl config section is set but the flowControl feature gate is disabled; its settings (other than saturationDetector) are ignored")
	}
```

**只有一行 Info 级 WARNING**，不是错误。这是 [07 篇 §3 静默失效总表](07-部署示例与端到端Demo.md#3-静默失效模式总表)上的一条：配了 `flowControl:` 但忘了开 gate，除 `saturationDetector` 外全部静默忽略。

## 8. 自己写一个插件

最小可用的 Scorer：

```go
package myscorer

import (
	"context"
	"encoding/json"

	fwkplugin "github.com/llm-d/llm-d-router/pkg/epp/framework/interface/plugin"
	fwksched "github.com/llm-d/llm-d-router/pkg/epp/framework/interface/scheduling"
	dlmetrics "github.com/llm-d/llm-d-router/pkg/epp/framework/plugins/datalayer/extractor/metrics"
)

const MyScorerType = "my-scorer"

type MyScorer struct {
	typedName fwkplugin.TypedName
}

func (s *MyScorer) TypedName() fwkplugin.TypedName { return s.typedName }

func (s *MyScorer) Category() fwksched.ScorerCategory { return fwksched.Distribution }

// Consumes 必须声明要读的数据，否则运行时读取会被 Scope 拦下并静默丢弃，
// 只体现在 plugin_data_scope_violations_total 指标上。
func (s *MyScorer) Consumes() fwkplugin.DataDependencies {
	return fwkplugin.DataDependencies{
		Required: map[fwkplugin.DataKey]any{
			fwkplugin.NewDataKey(dlmetrics.RunningRequestsSizeKey, dlmetrics.MetricsExtractorType): int(0),
		},
	}
}

func (s *MyScorer) Score(ctx context.Context, request *fwksched.InferenceRequest,
	pods []fwksched.Endpoint) map[fwksched.Endpoint]float64 {
	scores := make(map[fwksched.Endpoint]float64, len(pods))
	for _, p := range pods {
		m := p.GetMetrics()
		if m == nil {
			scores[p] = 0
			continue
		}
		// 返回值必须落在 [0,1]；框架会再 clamp 一次。
		scores[p] = 1.0 / float64(1+m.RunningRequestsSize)
	}
	return scores
}

func Factory(name string, params *json.Decoder, handle fwkplugin.Handle) (fwkplugin.Plugin, error) {
	return &MyScorer{typedName: fwkplugin.TypedName{Type: MyScorerType, Name: name}}, nil
}
```

注册（加到 `registerInTreePlugins` 或自己的 runner 里）：

```go
fwkplugin.Register(myscorer.MyScorerType, fwkplugin.StabilityAlpha, myscorer.Factory)
```

**四个容易忘的点**：

1. **`Consumes()` 必须声明**，否则读取被静默丢弃（01 篇 §7 最后一条）
2. **返回值必须在 `[0,1]`**，虽然框架会 clamp，但超范围意味着你的权重语义已经乱了
3. **`Alpha` 稳定性需要 `--allow-experimental-plugins`**，否则启动失败
4. **参数结构体要能被严格解析**（`DisallowUnknownFields`），字段名和 YAML 必须完全一致

想在编译期加插件而不 fork 上游，也可以走「自建 runner + import 上游包」的路子——和 [kube-scheduler 06 篇](../../../scheduling/kube-scheduler/06-扩展实战-自定义调度插件开发.md) 的 out-of-tree 插件思路一致。

## 9. 速查

### 五类核心扩展点一句话

| 扩展点 | 一句话 | 组合语义 | 空集后果 |
|-------|-------|---------|---------|
| Screener | Director 层的全局硬约束 | **交集** | 503 |
| Filter | profile 内的硬约束 | **顺序 pipeline** | 429 |
| Scorer | 软偏好，`[0,1]` | **加权求和**（不归一化） | — |
| Picker | 从打分结果里挑 | 每 profile 一个 | — |
| ProfileHandler | 决定跑哪些 profile、谁是 primary | 全局**唯一** | 启动失败 |

### 关键文件

```
pkg/epp/framework/interface/scheduling/plugins.go:43-78     四个调度接口
pkg/epp/framework/interface/requestcontrol/plugins.go:50-131 五个请求控制接口
pkg/epp/framework/interface/plugin/registry.go:26-72        注册表
cmd/epp/runner/runner.go:573-736                            所有内建插件的注册（读这里知道有哪些插件）
pkg/epp/config/loader/configloader.go:276-298               实例化
pkg/epp/config/loader/defaults.go:124-141                   默认注入
pkg/epp/scheduling/scheduler_profile.go:244-255             加权求和那一行
pkg/epp/framework/plugins/README.md                         官方插件清单文档
```

### 配置坑清单（本篇涉及的）

| 坑 | 症状 | 修法 |
|----|------|------|
| 用了 `precise-prefix-cache-scorer` | 启动日志有 DEPRECATION | 换成 `precise-prefix-cache-producer` + `prefix-cache-scorer` |
| 只配 `prefix-cache-scorer` 不配 producer | 静默用了**近似**索引 | 想要精确必须显式配 producer |
| 配了 `flowControl:` 但没开 gate | 除 saturationDetector 外全忽略，仅一行 Info WARNING | 同时设 `featureGates: ["flowControl"]` |
| `utilization-filter` 未设 `fallbackOnEmpty` | 尖峰时整池 429 | 生产环境建议开 |
| DP 池的 `metrics-data-source` 设了 `port` | 所有 rank 抓同一端口，指标全错不报错 | 不要设 `port` |
| Alpha 插件未加 `--allow-experimental-plugins` | 启动失败 | 加 flag（并想清楚是否该用 Alpha） |
| 两个 ProfileHandler / 多 profile 配 single | 启动失败（这是好事） | 按 §6.3 改 |

---

**上一篇**：[01 · 请求的一生](01-请求的一生-主控制流.md) ｜ **下一篇**：[03 · Data Layer 与指标采集](03-核心代码分析-DataLayer与指标采集.md)
